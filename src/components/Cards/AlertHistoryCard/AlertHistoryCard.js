import Card from "@mui/material/Card";
import {CardContent} from "@mui/material";


export default function AlertHistoryCard({alert}) {

    return (
        <Card className={"cards"}>
            <CardContent>Alert1</CardContent>
        </Card>
    )

}